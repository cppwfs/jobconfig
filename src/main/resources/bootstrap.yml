spring:
  application:
    name: fooconfig
#  cloud:
#    config:
#      uri: http://localhost:8888
  cloud:
     config:
        discovery:
           enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9395/eureka/
